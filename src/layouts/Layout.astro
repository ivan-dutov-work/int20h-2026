---
import { getImage } from "astro:assets";
import textureImage from "../assets/backgrounds/texture.webp";

const optimizedBackground = await getImage({
  src: textureImage,
  format: "webp",
});
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>INT20H Хакатон 2026</title>
    <meta
      name="description"
      content="Приєднуйся до INT20H Hackathon 2026 — найбільшого студентського IT‑хакатону в Україні. 15–16 березня у Києві. Реєструйся, пройди відбірковий тест і покажи свої навички в Web, AI, Data Science та UI/UX Design."
    />
    <meta
      name="keywords"
      content="INT20H, хакатон, ІТ, AI, штучний інтелект, конкурс програмування, студентський хакатон 2026, Київ"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="canonical" href={Astro.url.origin + Astro.url.pathname} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url.origin + Astro.url.pathname} />
    <meta property="og:title" content="INT20H Хакатон 2026" />
    <meta
      property="og:description"
      content="Найкращі студентські команди змагатимуться у технічних завданнях з Web, AI, Data Science та UI/UX Design. Пройди відбірковий тест і потрап до фіналу."
    />
    <meta
      property="og:image"
      content={Astro.url.origin + '/og-image.png'}
    />
    <meta property="og:image:width" content="2314" />
    <meta property="og:image:height" content="1823" />
    <meta property="og:site_name" content="INT20H 2026" />
    <meta property="og:locale" content="uk_UA" />
    <meta name="generator" content={Astro.generator} />

    <style define:vars={{ bgUrl: `url("${optimizedBackground.src}")` }}></style>
  </head>

  <body>
    <slot />

    <script>
      // Using 'load' ensures all other assets (CSS, JS, Hero Images) are done first
      window.addEventListener("load", () => {
        document.body.classList.add("bg-loaded");
      });
    </script>
  </body>
</html>

<style>
  /* Base styles (hidden by default) */
  body::before {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    /* No background-image here initially! */
    background-repeat: repeat;
    opacity: 0; /* Start invisible to prevent flash */
    pointer-events: none;
    z-index: -1;
    transition: opacity 0.5s ease-in; /* Smooth fade-in */
  }

  /* 5. The class that applies the image and reveals it */
  body.bg-loaded::before {
    /* Use the variable from frontmatter */
    background-image: var(--bgUrl);
    opacity: 0.1; /* Your desired opacity */
  }
</style>
